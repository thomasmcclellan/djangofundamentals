# Templates contain the static parts of the HTML page (like a scaffolding)

# Template tags (Django Template Variables) have their own special syntax
# This syntax allows you to inject dynamic content that your Django app's views will produce, effecting the final HTML